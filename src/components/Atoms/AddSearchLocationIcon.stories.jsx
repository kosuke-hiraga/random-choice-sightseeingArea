import AddSearchLocationIcon from "./AddSearchLocationIcon";

export default {
    title: 'Atoms/AddSearchLocationIcon',
    component: AddSearchLocationIcon,
}

const Template = (args) => <AddSearchLocationIcon />
export const Icon = Template.bind({});